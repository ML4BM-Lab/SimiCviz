% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SimiCviz-package.R
\name{setLabelNames}
\alias{setLabelNames}
\title{Set or update label names and colors}
\usage{
setLabelNames(x, label_names, colors = NULL)
}
\arguments{
\item{x}{\code{SimiCvizExperiment}.}

\item{label_names}{named character vector mapping labels to display names.}

\item{colors}{optional named character vector mapping labels to colors.}
}
\value{
Modified \code{SimiCvizExperiment} object.
}
\description{
Validates that the provided vectors match the number of labels
in \code{cell_labels} (preferred) or \code{weights}.
}
\examples{
wt <- data.frame(tf     = c("TF1", "TF2"),
                   target = c("Gene1", "Gene2"), 
                   weight = c(0.5, 0.8),
                   label  = c(0, 1)
                   )
 cell_labels <- data.frame(cell = c("Cell1", "Cell2"), 
                          label = c(0, 1)
                         )
simic <- SimiCvizExperiment(weights = wt, cell_labels = cell_labels)
simic <- setLabelNames(simic, 
                       label_names = c("control", "treated"),
                       colors = c("#e0e0e0", "#a8c8ff"))
}
