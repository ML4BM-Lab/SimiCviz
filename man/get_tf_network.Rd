% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_network_heatmap.R
\name{get_tf_network}
\alias{get_tf_network}
\title{Extract stacked TF-target weight matrix across labels}
\usage{
get_tf_network(x, tf_name, labels = NULL, r2_threshold = NULL)
}
\arguments{
\item{x}{A \code{SimiCvizExperiment} object.}

\item{tf_name}{Character; name of the transcription factor.}

\item{labels}{Integer vector of labels to include (default: all).}

\item{r2_threshold}{Numeric or \code{NULL}. If non-NULL, weights for
targets with adjusted R² below this value are set to \code{NA}.}
}
\value{
A data.frame with target genes as rows and one column per label
(named by \code{label_names}).
}
\description{
For a given TF, extracts the regulatory weight for every target gene in each
label. Targets whose adjusted R² falls below \code{r2_threshold} in a given
label receive \code{NA} (analogous to Python's \code{get_TF_network(...,
stacked=TRUE)}).
}
\examples{
 # Example usage
  simic <- readRDS(system.file("extdata", "simic_full.rds", package = "SimiCviz"))
  network <- get_tf_network(simic, simic@tf_ids[1], labels = c(1, 2), r2_threshold = 0.7)
  print(network)

}
