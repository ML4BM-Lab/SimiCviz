% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_bar.R
\name{plot_tf_weights}
\alias{plot_tf_weights}
\title{Plot weight barplots for transcription factors}
\usage{
plot_tf_weights(
  x,
  tf_names = NULL,
  labels = NULL,
  top_n = 50L,
  allowed_targets = NULL,
  grid = c(4L, 1L),
  save = FALSE,
  filename = NULL,
  out_dir = getwd(),
  width = 16,
  height = NULL
)
}
\arguments{
\item{x}{\code{SimiCvizExperiment}}

\item{tf_names}{character vector of TFs to plot (default: all)}

\item{labels}{integer or character vector of labels (default: all)}

\item{top_n}{integer; show only the top N targets by mean absolute weight
(default 50). Applied after \code{allowed_targets} filtering.}

\item{allowed_targets}{character vector or NULL; restrict targets to this
pre-filtered set before applying \code{top_n}. Useful for passing targets
that pass an external significance filter.}

\item{grid}{integer vector \code{c(nrow, ncol)} per page (default
\code{c(4, 1)}). Set \code{NULL} for a single page.}

\item{save}{logical; save to PDF (default FALSE)}

\item{filename}{PDF filename (default \code{"TF_weights.pdf"})}

\item{out_dir}{output directory (default \code{getwd()})}

\item{width, height}{page dimensions in inches}
}
\value{
Invisibly, a list of page grobs.
}
\description{
For each TF, shows a grouped bar chart of regulatory weights across all
target genes, coloured by phenotype label. Mirrors
\code{SimiCVisualization.plot_tf_weights} from the Python package.
}
\examples{
simic <- readRDS(system.file("extdata", "simic_full.rds", 
                 package = "SimiCviz"))
plot_tf_weights(simic, tf_names =c("Pms1","Tet2"),
                top_n = 20, grid = c(2, 1))
}
