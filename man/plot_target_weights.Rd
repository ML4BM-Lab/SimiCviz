% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_bar.R
\name{plot_target_weights}
\alias{plot_target_weights}
\title{Plot weight barplots for target genes}
\usage{
plot_target_weights(
  x,
  target_names = NULL,
  labels = NULL,
  grid = c(4L, 1L),
  top_n = NULL,
  save = FALSE,
  filename = NULL,
  out_dir = getwd(),
  width = 12,
  height = NULL
)
}
\arguments{
\item{x}{\code{SimiCvizExperiment}}

\item{target_names}{character vector of targets to plot (default: all)}

\item{labels}{integer or character vector of labels (default: all)}

\item{grid}{integer vector \code{c(nrow, ncol)} per page (default
\code{c(4, 1)}). Set \code{NULL} for a single page.}

\item{top_n}{Optional integer. Currently accepted for API compatibility;
ignored in target mode.}

\item{save}{logical; save to PDF (default FALSE)}

\item{filename}{PDF filename (default \code{"target_weights.pdf"})}

\item{out_dir}{output directory (default \code{getwd()})}

\item{width, height}{page dimensions in inches}
}
\value{
Invisibly, a list of page grobs.
}
\description{
For each target gene, shows a grouped bar chart of incoming regulatory
weights from all TFs, coloured by phenotype label. Mirrors
\code{SimiCVisualization.plot_target_weights} from the Python package.
}
\examples{
simic <- readRDS(system.file("extdata", "simic_full.rds", 
                 package = "SimiCviz"))
plot_target_weights(simic, 
                    target_names = c("Brinp3", "Top2a"), 
                    labels = c(0, 1), grid = c(2, 1))
}
