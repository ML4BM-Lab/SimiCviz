% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_r2_distribution.R
\name{plot_r2_distribution}
\alias{plot_r2_distribution}
\title{Plot adjusted R² distributions}
\usage{
plot_r2_distribution(
  adjusted_r_squared,
  x = NULL,
  labels = NULL,
  threshold = 0.7,
  grid = NULL,
  save = FALSE,
  filename = NULL,
  out_dir = getwd(),
  width = 10,
  height = NULL
)
}
\arguments{
\item{adjusted_r_squared}{A \strong{named list} of numeric vectors, one per
label.
Names must be label identifiers (e.g. \code{"0"}, \code{"1"}, …).
This metric is specific to SimiC's regression step; other methods may not
produce it, so it is kept as an explicit input rather than extracted from
the experiment object.}

\item{x}{Optional \code{SimiCvizExperiment} object used solely to resolve
display names and colors for labels.
If \code{NULL}, default \code{"Label i"} naming and a built-in palette are
used.}

\item{labels}{Optional vector of labels to plot (subset of
\code{names(adjusted_r_squared)}). Defaults to all.}

\item{threshold}{Numeric R² threshold line and summary-statistic cutoff
(default \code{0.7}).}

\item{save}{logical; save to PDF (default \code{FALSE}).}

\item{filename}{PDF filename (default \code{"R2_distributions.pdf"}).}

\item{out_dir}{output directory (default \code{getwd()}).}

\item{width, height}{page dimensions in inches (defaults 10 × 5*nrow).}

\item{nrow, ncol}{Optional grid layout. If both \code{NULL}, one label per
row.}
}
\value{
Called for side effects (plots). Returns \code{invisible(NULL)}.
}
\description{
Plots histograms of adjusted R² values per label, similar to the Python
\code{SimiCVisualization$plot_r2_distribution}.
}
