% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SimiCvizExperiment-utils.R
\name{calculate_activity_scores}
\alias{calculate_activity_scores}
\title{Calculate activity scores from SimiCvizExperiment weights}
\usage{
calculate_activity_scores(
  simic,
  expression = NULL,
  adj_r2_threshold = NULL,
  sort_by = "expression",
  select_top_k = NULL,
  percent_of_target = 1,
  n_cores = 1,
  backend = "sequential",
  verbose = TRUE
)
}
\arguments{
\item{simic}{SimiCvizExperiment object with weights and expression data}

\item{expression}{expression matrix (cells x genes). If NULL, uses data from
metadata or file path if available.}

\item{adj_r2_threshold}{minimum adjusted R2 for target filtering (default: 0.7)}

\item{sort_by}{sorting criterion ("expression", "weight", or "adj_r2")}

\item{select_top_k}{keep only top K targets per TF (NULL = all)}

\item{percent_of_target}{percentage of targets to use (0-1)}

\item{n_cores}{number of cores for parallelization (1 = sequential)}

\item{backend}{parallelization backend ("sequential", "multicore", "multisession")}

\item{verbose}{print progress messages}
}
\value{
SimiCvizExperiment with computed AUC scores added to @auc slot
}
\description{
Efficiently computes TF activity scores for all cells using label-specific
GRNs. Avoids redundant computation by calculating only for each cell's
corresponding label.
}
