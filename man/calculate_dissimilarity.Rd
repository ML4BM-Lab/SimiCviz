% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dissimilarity.R
\name{calculate_dissimilarity}
\alias{calculate_dissimilarity}
\title{Calculate dissimilarity scores across phenotype labels}
\usage{
calculate_dissimilarity(
  x,
  labels = NULL,
  cell_groups = NULL,
  n_breaks = 100L,
  verbose = TRUE
)
}
\arguments{
\item{x}{A \code{SimiCvizExperiment} object.}

\item{labels}{Integer vector of label keys or character vector of label
display names to compare (default: all labels). Mixing types is allowed.}

\item{cell_groups}{Optional named list mapping group names to character
vectors of cell identifiers.}

\item{n_breaks}{Number of histogram bins (default 100).}

\item{verbose}{Logical; print progress messages (default \code{TRUE}).}
}
\value{
A \code{data.frame} with TFs as rows, sorted by score (descending).
}
\description{
Compares per-cell TF activity-score distributions between phenotype labels
using a histogram-based MinMax divergence.
}
\examples{
simic <- readRDS(system.file("extdata", "simic_full.rds", 
         package = "SimiCviz"))
dis_score <- calculate_dissimilarity(simic)
}
