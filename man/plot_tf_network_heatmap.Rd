% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_network_heatmap.R
\name{plot_tf_network_heatmap}
\alias{plot_tf_network_heatmap}
\title{Plot heatmap of a TF regulatory network across phenotypes}
\usage{
plot_tf_network_heatmap(
  x,
  tf_name,
  labels = NULL,
  top_n = 10L,
  r2_threshold = NULL,
  cmap = c("blue", "white", "red"),
  show_values = TRUE,
  save = FALSE,
  filename = NULL,
  out_dir = getwd(),
  width = NULL,
  height = NULL
)
}
\arguments{
\item{x}{A \code{SimiCvizExperiment} object.}

\item{tf_name}{Character; name of the transcription factor to plot.}

\item{labels}{Integer or character vector of labels to include (default:
all).}

\item{top_n}{Integer; number of top target genes to display, ranked by
maximum absolute weight across labels (default: 10).}

\item{r2_threshold}{Numeric or \code{NULL}. Targets with adjusted R² below
this value receive \code{NA} and are rendered as grey tiles. Passed to
\code{.get_tf_network()}.}

\item{cmap}{Colour palette specification, identical to
\code{\link{plot_dissimilarity_heatmap}}. Recommended diverging palettes
such as \code{"RdBu_r"} (red-white-blue, reversed) or a two-colour
vector like \code{c("blue", "white", "red")}. Default: \code{c("blue",
  "white", "red")}.}

\item{show_values}{Logical; annotate each cell with its weight or
\emph{"< R² threshold"} text (default \code{TRUE}).}

\item{save}{Logical; save the plot to a PDF file (default \code{TRUE}).}

\item{filename}{Custom filename (default: auto-generated from
\code{tf_name}).}

\item{out_dir}{Output directory for the PDF (default: working directory).}

\item{width, height}{PDF dimensions in inches (auto-calculated if
\code{NULL}).}
}
\value{
Invisibly, a list with components:
\describe{
\item{plot}{The \code{ggplot} object.}
\item{data}{The (filtered) data.frame of weights used for the plot.}
}
}
\description{
Displays a heatmap showing the regulatory weights of a single transcription
factor across its top target genes, with one column per phenotype label.
Targets that fail the adjusted-R² filter are shown as grey cells labelled
\emph{"< R² threshold"}, mirroring the Python
\code{SimiCVisualization.plot_tf_network_heatmap()} method.
}
\examples{
\dontrun{
  # Basic usage
  plot_tf_network_heatmap(simic, "Gata2")

  # Custom palette and top targets
  plot_tf_network_heatmap(simic, "Zfp950", top_n = 15,
                          cmap = "RdBu_r", r2_threshold = 0.7)

  # Only specific labels, no saving
  plot_tf_network_heatmap(simic, "Runx2", labels = c(0, 3),
                          save = FALSE)
}

}
